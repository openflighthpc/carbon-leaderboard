<h1 id="welcome">Making HPC greener</h1>
<p id="welcome-tagline">
  Finding the most carbon efficient HPC solutions by ranking the emissions of real servers.
</p>
<div class="home-top-section my-5">
  <div class="counter-wrapper">
    <p class="counter"><%= @num_devices %></p>
    <p class="big-text">servers entered</p>
  </div>
  <div class="cl-card flex-column-centred">
    <h3>Top most carbon efficient servers</h3>
    <p>Ranked by estimated equivalent CO<sub>2</sub> emissions per core at full load.</p>

    <div class="home-leaderboard leaderboard-wrapper mt-4">
      <div class="leaderboard-content-wrapper">
        <% (1..3).each do |rank| %>
          <div class="leaderboard-item-wrapper blurred-frame rank-<%= rank %>">
            <div class="leaderboard-item glare"></div>
            <div class="leaderboard-item rank-column"><%= rank %></div>
            <div>
              <span class="big-text"><%= @devices[rank - 1][:uuid].first(8) %></span><br>
              <span><%= @devices[rank - 1][:user] %></span>
            </div>
            <div></div>
            <div><span class="flag fi fi-<%= @devices[rank - 1][:location].downcase %> fis"></span></div>
          </div>
        <% end %>
      </div>
    </div>
    <%= button_to "View full leaderboard", leaderboard_path, class: 'button mt-3', method: :get %>
  </div>
  <div class="counter-wrapper">
    <p class="counter"><%= @num_configs %></p>
    <p class="big-text">different server configurations</p>
  </div>
</div>

<script type="text/javascript" src="/assets/report/glare.js"></script>
